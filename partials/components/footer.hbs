<footer class="gh-footer{{#match @custom.header_and_footer_color "Accent color"}} has-accent-color{{/match}} gh-outer">
    <div class="gh-footer-inner gh-inner">

        <div class="gh-footer-bar">
            <span class="gh-footer-logo is-title">
                {{#if @site.logo}}
                    <picture>
                        <source 
                            srcset="{{img_url @site.logo size="xs" format="avif"}} 1x,
                                    {{img_url @site.logo size="s" format="avif"}} 2x"
                            type="image/avif"
                        >
                        <!-- Serve the WebP format if the browser supports it -->
                        <source 
                            srcset="{{img_url @site.logo size="xs" format="webp"}} 1x,
                                    {{img_url @site.logo size="s" format="webp"}} 2x"
                            type="image/webp"
                        >
                        <!-- Serve original file format as a fallback -->
                        <img
                            srcset="{{img_url @site.logo size="xs"}} 1x,
                                    {{img_url @site.logo size="s"}} 2x"
                            src="{{img_url @site.logo size="s"}}"
                            alt="{{@site.title}}"
                        >
                    </picture>
                {{else}}
                    {{@site.title}}
                {{/if}}
            </span>
            <nav class="gh-footer-icons">
                <ul class="social-icons">
                    {{#if @custom.x}}
                        <li><a href="{{@custom.x}}" class="icon" title="X">{{> "icons/x"}}</a></li>
                    {{/if}}

                    {{#if @custom.mastodon}}
                        <li><a href="{{@custom.mastodon}}" class="icon" title="Mastodon">{{> "icons/mastodon"}}</a></li>
                    {{/if}}

                    {{#if @custom.instagram}}
                        <li><a href="{{@custom.instagram}}" class="icon" title="Instagram">{{> "icons/instagram"}}</a></li>
                    {{/if}}

                    {{#if @custom.tiktok}}
                        <li><a href="{{@custom.tiktok}}" class="icon" title="TikTok">{{> "icons/tiktok"}}</a></li>
                    {{/if}}

                    {{#if @custom.youtube}}
                        <li><a href="{{@custom.youtube}}" class="icon" title="YouTube">{{> "icons/youtube"}}</a></li>
                    {{/if}}

                    {{#if @custom.facebook}}
                        <li><a href="{{@custom.facebook}}" class="icon" title="Facebook">{{> "icons/facebook"}}</a></li>
                    {{/if}}

                    {{#if @custom.podcast}}
                        <li><a href="{{@custom.podcast}}" class="icon" title="Podcast">{{> "icons/podcast"}}</a></li>
                    {{/if}}
                </ul>
            </nav>
            <div class="gh-footer-menu">
                {{navigation type="secondary"}}
            </div>
        </div>
        
        {{#if @custom.show_newsletter_signup_form}}
            {{#if @site.members_enabled}}
                {{#unless @member}}
                    <section class="gh-footer-signup">
                        <h2 class="gh-footer-signup-header is-title">
                            {{#if @custom.signup_heading}}{{@custom.signup_heading}}{{else}}{{@site.title}}{{/if}}
                        </h2>
                        <p class="gh-footer-signup-subhead is-body">
                            {{#if @custom.signup_subheading}}{{@custom.signup_subheading}}{{else}}{{@site.description}}{{/if}}
                        </p>
                        {{> "email-subscription"}}
                    </section>
                {{/unless}}
            {{/if}}
        {{/if}}

    </div>
</footer>